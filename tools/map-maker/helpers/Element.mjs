class Element {
  constructor(type, spriteId, x, y, width, height) {

    this.type = type;
    this.spriteId = spriteId;
    
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
}

export default Element;